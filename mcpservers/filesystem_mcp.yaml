name: Filesystem MCP Server
dcc_uri: dev/mcpservers/filesystem_mcp
description: Local filesystem access with configurable allowed paths
version: 1.0.0
schema: v1
dcc_tags:
  - filesystem

mcpServers:
  - name: Filesystem
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-filesystem"
      - "${{ inputs.PATH }}"
