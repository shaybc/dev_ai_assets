name: Docker MCP Server
dcc_uri: dev/mcpservers/docker_mcp
description: MCP server that runs via Docker’s MCP gateway, enabling Continue to access containerized MCP tools through the local Docker runtime.
version: 1.0.0
schema: v1
dcc_tags:
  - docker

mcpServers:
  - name: DOCKER
    command: docker
    args:
      - mcp
      - gateway
      - run
