name: Gemini Embeddings via Shim
dcc_uri: dev/models/gemini_embedding
version: "1.2"
schema: v1
description: DCC server routing to model Gemini embeddings through an OpenAI-compatible shim
dcc_tags:
  - gemini_embedding
models:
  - name: Gemini Embeddings
    provider: openai
    model: text-embedding-004
    apiBase: http://localhost:7331/v1
    roles:
      - embed
